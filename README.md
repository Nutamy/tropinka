# Тропинка — лендинг (Cloudflare Pages)

Статический сайт + одна serverless-функция (Cloudflare Pages Functions), которая
пересылает заявки из формы на сайте в Telegram. Внешних зависимостей и сборки нет —
это чистые HTML/CSS/JS, деплой заключается в загрузке папки как есть.

## Структура проекта

```
tropinka-landing/
├── index.html              — весь лендинг (одна страница)
├── css/styles.css          — UI Kit: палитра, типографика, компоненты
├── js/main.js               — WhatsApp-ссылки, мобильное меню, отправка формы
├── functions/api/submit.js  — Cloudflare Pages Function: форма → Telegram
├── images/                  — логотип, favicon и фото галереи «Жизнь центра»
├── _headers                 — базовые security-заголовки и кеширование
└── README.md
```

## 1. Деплой на Cloudflare Pages

1. Залейте эту папку в Git-репозиторий (GitHub/GitLab) — Cloudflare Pages
   деплоится из репозитория. Либо используйте прямой деплой через `wrangler`:
   ```bash
   npx wrangler pages deploy tropinka-landing --project-name=tropinka
   ```
2. В Cloudflare Dashboard → **Workers & Pages → Create → Pages** подключите
   репозиторий. Build command — оставить пустым, output directory — `/`
   (корень проекта, так как сборки нет).
3. Cloudflare сам обнаружит папку `functions/` и поднимет `/api/submit`
   как serverless-функцию — ничего дополнительно настраивать не нужно.

## 2. Настройка Telegram-бота

1. В Telegram напишите **@BotFather** → `/newbot`, задайте имя — получите
   `TELEGRAM_BOT_TOKEN` вида `123456789:AAExxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`.
2. Узнайте `TELEGRAM_CHAT_ID`:
   - Если заявки должны приходить лично вам — напишите своему боту любое
     сообщение, затем откройте
     `https://api.telegram.org/bot<TOKEN>/getUpdates` в браузере и найдите
     `"chat":{"id": ...}` в ответе.
   - Если заявки должны приходить в группу — добавьте бота в группу,
     напишите в группе любое сообщение и точно так же посмотрите
     `getUpdates` (id группы будет отрицательным числом).
3. Отправьте боту `/start` (или любое сообщение) до первого теста формы —
   иначе Telegram не разрешит боту писать первым.

## 3. Переменные окружения в Cloudflare

В Cloudflare Dashboard → ваш проект Pages → **Settings → Environment variables**
добавьте для окружения **Production** (и при необходимости Preview):

| Имя переменной        | Тип      | Значение                          |
|------------------------|----------|------------------------------------|
| `TELEGRAM_BOT_TOKEN`   | Secret   | токен от @BotFather                |
| `TELEGRAM_CHAT_ID`     | Secret   | id чата/группы для приёма заявок   |

После добавления переменных — **Retry deployment** (переменные подхватываются
только новым деплоем).

## 4. Что уже сделано в коде

- **Все CTA ведут в WhatsApp** на номер `+7 777 196 09 89` — единый номер
  задан в одном месте (`WHATSAPP_NUMBER` в `js/main.js`). У каждой кнопки
  свой контекстный текст сообщения (`data-msg` в HTML) — привязан к тому
  офферу/блоку, где стоит кнопка.
- **Форма заявки** отправляется на `/api/submit` (без перезагрузки страницы)
  и оттуда пересылается в Telegram. Есть honeypot-поле против спам-ботов.
- **Дизайн** — по официальному UI Kit проекта (палитра, Comfortaa/Nunito,
  pill-радиусы, мягкие цветные тени) в духе Impeccable.
- **2GIS**: ссылка `https://go.2gis.com/hM5Co` используется в hero, разделе
  «Почему мы?», отзывах, локации и футере — весь адрес и упоминания 2ГИС
  кликабельны.
- **Instagram**: ссылка на `https://www.instagram.com/tropka_log` в разделе
  «Жизнь центра» (галерея из 11 реальных фото центра) и в футере.
- **Логотип**: настоящий логотип (`images/logo.png`) используется в шапке,
  футере, секции «О центре» и как favicon (`images/favicon.png`).
- **Соцсети**: Facebook, Instagram, Telegram и WhatsApp — иконки-ссылки в
  футере; галерея «Жизнь центра» — кликабельные фото с лайтбоксом.
- **Навигация**: шапка и мобильное меню — 8 пунктов (О нас, Как проходит
  занятие, Цены, Почему мы?, Отзывы, Педагоги, Фото, Адрес), все ведут на
  соответствующие `id` на странице.

## 5. Что нужно доделать вручную перед публикацией

- [ ] **Цена мини-группы 3–5 лет** — точной суммы не было в проектных
      материалах, поэтому кнопка ведёт на «Узнать цену» в WhatsApp вместо
      цифры. Впишите цену в `.program-price`, если она известна.
- [ ] Сверить дословность остальных цитат отзывов по актуальной странице
      2ГИС (имя «Салтанат Есендыковна» в разделе «Педагоги» и в отзывах уже
      приведено к единому написанию по данным клиента).
- [ ] По запросу из чата ценовой якорь («Сколько это стоило бы по отдельности»)
      и блок «Родительские тревоги» убраны с лендинга целиком (не только
      скрыты — вырезаны из `index.html`). Если решите вернуть якорь позже,
      напомните — верну и впишем реальные цифры конкурентов.

## 6. История изменений (раунды правок после первой версии)

**Раунд 2** — чистка контента и реальные фото/логотип:
- Убраны: статы «9+ лет разработки методики» и «100+ источников», блок
  «Родительские тревоги», блок-якорь цен «Сколько это стоило бы по отдельности»,
  подпись «Точечный запрос» на карточках Логопед/Изостудия (сами карточки
  остались), слова «бесплатное»/«без обязательств» в финальном CTA.
- Добавлен раздел «О центре» (текст из проектного документа), раздел
  «Почему мы?» (5 карточек, адрес кликабелен → 2ГИС), галерея «Жизнь центра»
  на 11 реальных фото вместо цветных плейсхолдеров.

**Раунд 3** — редизайн под фирменный синий фон и соцсети:
- Фон всего сайта — фирменный градиент `#6AC6ED`, заголовки/подписи на
  синем — белые; на первом экране — иллюстрация вместо статов.
- Логотип увеличен и читаем (белая подложка под прозрачным PNG) — в шапке,
  футере и разделе «О центре».
- Галерея переделана в «пузырьковую» раскладку (шары разного размера) с
  лайтбоксом по клику (открытие/закрытие по кнопке, клику по фону и Esc).
- Добавлен раздел «Мы в соцсетях» в футере — Facebook, Instagram, Telegram,
  WhatsApp (иконки без сторонних брендовых цветов — по правилу UI Kit
  «один цвет = одна роль»).
- Заголовок «Больше фото и видео в Instagram» стал кликабельной ссылкой.

**Раунд 4** — структура и навигация:
- Убран дублирующий блок статов под hero («15 лет» / «5.0★»
  повторяли то, что уже показано в чипах хиро) — вместо этого чипы в hero
  укрупнены.
- Раздел «О центре» разделён на три визуально разных блока: интро-текст,
  сетка иконок «Развиваем» (цветные бейджи вместо списка) и — отдельно —
  список «Как проходят занятия», перенесённый в раздел «Как проходит
  занятие» в виде карточек с иконками (третья, самостоятельная
  визуализация, отличная от «шагов» Мысль→Движение→Творчество и от
  цифр-статов).
- Карточки «Логопед» и «Изостудия» расширены (мета-теги формата, список
  преимуществ — по 3 пункта на карточку).
- Шапка и мобильное меню приведены к 8 пунктам в порядке: О нас · Как
  проходит занятие · Цены · Почему мы? · Отзывы · Педагоги · Фото · Адрес.
  У раздела галереи добавлен `id="gallery"` для якоря «Фото».
- Исправлена вёрстка шапки на узких экранах (320–480px) — уменьшены
  отступы/шрифт кнопки «Записаться» и логотипа, чтобы шапка не выходила
  за пределы экрана; убран `white-space: nowrap` у `.btn`, чтобы длинный
  текст кнопок мог переноситься, а не растягивать страницу по горизонтали.
- Изображение на первом экране сжато (1400px → 1000px, ~300 КБ → ~165 КБ)
  без потери резкости на реальном размере показа — для скорости загрузки.

**Раунд 5** — сокращение программ и снятие акцента на «бесплатно»:
- Из блока цен убраны карточки «Мини-группа 1–3 года» и «Мини-группа 5–7 лет».
  Остались: «Подготовка к школе», «Мини-группа 3–5 лет», «Логопед», «Изостудия»
  (сетка 2×2 вместо 4+2).
- Опция «1–3 года» убрана из выпадающего списка в форме заявки — она
  соответствовала удалённой карточке.
- По всему сайту убрано слово «бесплатное» перед «пробное занятие» — во
  всех WhatsApp-сообщениях кнопок, в hero, в `<meta name="description">` и
  `og:description`. Понятие «пробное занятие» как таковое осталось.

**Раунд 6** — Rich Link Preview / Open Graph и уточнение мини-групп:
- В `<head>` `index.html` добавлен полный набор мета-тегов для превью ссылки
  в мессенджерах и соцсетях: Open Graph (`og:url`, `og:site_name`, `og:image`
  + `width/height/type/alt`, `og:locale`), Twitter Card
  (`summary_large_image`) и `<link rel="canonical">`. Домен зашит как
  `https://tropinka.pages.dev/` — замените на реальный при другом домене.
- Добавлена картинка `images/og-image.jpg` (1200×630, JPEG ~127 КБ) — фото
  детей с бумажными голубками на фирменном фоне `#6AC6ED`, специально
  подготовлено под формат превью (без обрезки персонажей).
- В разделе «Как проходит занятие» карточка «Мини-группы по возрастам»
  исправлена с `1–3, 3–5 и 5–7 лет` на `3–5 и 5–7 лет` — упоминание группы
  1–3 года было рудиментом с раунда 5, где сама группа уже была убрана из
  цен и формы заявки.
- В блоке цен у карточки «Мини-группа 3–5 лет» мета-строка `60 минут` /
  `Мини-группа` заменена на `3 часа в день` / `5 дней в неделю` — тот же
  формат занятий, что и у флагманской программы «Подготовка к школе».

**Раунд 7** — читаемость и соответствие Impeccable (pbakaus/impeccable):
- Диагноз: почти весь сайт (не только hero) стоял на светло-голубом фирменном
  градиенте `#6AC6ED`, а текст поверх него был белым/светлым. По формуле
  контраста WCAG это ≈1.9:1 при минимуме 4.5:1 для обычного текста — то есть
  системная, а не косметическая проблема. Плюс несколько мест, где насыщенный
  цвет использовался как ЦВЕТ ТЕКСТА поверх другого насыщенного/светлого
  фона: жёлтый акцент в H1 на голубом (~1.3:1), звёзды отзывов `★★★★★`
  жёлтым на белом (~2.1:1), бейдж «Есть места» зелёным на светло-зелёном
  (~2.9:1), надпись `@tropka_log` жёлтым на бирюзовой кнопке (~2.7:1).
  Это прямое нарушение принципа Impeccable «Don't use gray/light text on
  colored backgrounds» — а `text-shadow`, которым местами пытались
  компенсировать контраст на заголовках, сам по себе является тревожным
  сигналом («shadow вместо реального контраста»), тоже не в духе Impeccable.
- **Фон**: `body` вместо сплошного фирменного градиента — белый. По
  официальной палитре роль небесного `#6AC6ED` — «фон бренда, шапка/хедер»,
  то есть он и не задумывался как фон всей страницы; это расширение
  появилось в раунде 3 и оказалось той самой причиной нечитаемости.
  Голубой остался в hero (баннер бренда) — с тёмным текстом поверх вместо
  белого. `#how-it-works`, `#why-us`, `#teachers`, `#location` получили
  еле заметный тон `var(--surface)` для ритма между секциями — вместо
  «всё на одном фоне» / «всё в тенях».
- **Hero**: `h1` — тёмно-чернильный (`--ink`, 6.8:1 на голубом) вместо
  белого; акцентное слово — `--sea-dark` (официальная роль «текст на
  цветном фоне», ≈4:1 на крупном жирном шрифте). Кнопка-призрак
  «Смотреть программы и цены» и адрес под hero — тоже перекрашены с белого
  на чернильный/`sea-dark`.
- **Мелкий текст на цветных подложках** приведён к WCAG AA: добавлены
  «текстовые» тона `--sun-dark` (`#8A6800`, вместо неиспользуемого дефолта
  `#E0A800`) для звёзд рейтинга и `--green-text` (`#1B783A`) для бейджа
  «Есть места», галочки «Подтверждён» и статуса формы — сама насыщенная
  `--sun`/`--green` остаётся на своих ролях (кнопка, фон бейджа), меняется
  только цвет текста поверх светлого тона той же палитры.
- **Прочее из чек-листа Impeccable**: тень в лайтбоксе `rgba(0,0,0,.55)`
  (чистый чёрный — отдельный «don't») заменена на тонированную
  `rgba(11,49,56,.55)`; цифра статистики (`--orange`, 34px) чуть затемнена
  (`#D9631A`), чтобы гарантированно проходить порог 3:1 для крупного текста.
- **Что оставлено намеренно, вопреки «умолчаниям» Impeccable**: pill-радиус
  кнопок/тегов (999px) и радиус карточек (20–24px) — это подтверждённый
  официальный UI Kit клиента, а не гипотеза; сам Impeccable формулирует
  правило «The brief wins» — подтверждённый бренд-кит имеет приоритет над
  дефолтными рекомендациями фреймворка.
- Body `line-height` увеличен с 1.55 до 1.62 (Impeccable: тексту нужен
  «воздух» между строк для комфортного чтения).
- Проверено скриншотами Playwright (desktop 1440px и mobile 390px,
  полная прокрутка) — до/после сравнение показало разницу принципиально:
  текст на голубом фоне, звёзды отзывов и бейдж «Есть места» стали
  отчётливо видны там, где раньше буквально терялись на фоне.

**Раунд 8** — реальные фото и имена педагогов:
- Раздел `#teachers` переведён с 4 плейсхолдеров на 6 карточек с реальными
  фото и данными: Татьяна Николаевна (логопед-дефектолог, 20+ лет),
  Анастасия Олеговна (педагог изостудии, 10+ лет), Анна Михайловна
  (основатель центра, логопед), Мисс Айдын (педагог английского языка,
  5+ лет), Ольга Викторовна (педагог, 25+ лет), Салтанат Есендыковна
  (педагог, 25+ лет).
- Фото сохранены в `images/teacher-*.jpg` (сжаты, макс. сторона 900px),
  выводятся через `<img>` с `object-fit: cover` в квадратном контейнере
  вместо svg-иконки-заглушки; убрана фраза-плейсхолдер под заголовком
  секции.
- Сетка секции переведена с `grid-4` на `grid-3` (добавлена адаптивная
  колонка на 640px) — 6 карточек ровно ложатся в 3×2 на десктопе и 2×3
  на планшете, вместо разъезжающихся 4+2.
- Написание имени «Салтанат Есендыковна» приведено к единому виду
  (по данным клиента) в обеих цитатах отзывов — ранее в тексте
  расходилось («Есендиковна» / «Есиндыковна»).

**Раунд 9** — единый формат карточек педагогов (по фидбэку «рамки должны
быть одинаковыми»):
- **Кадрирование фото**: 6 фото педагогов были разного типа (селфи крупным
  планом, студийный портрет, кадр в холле аэропорта) — при простом
  `object-fit: cover` лицо на одних фото занимало почти весь кадр, на
  других было мелким на фоне окружения. Все 6 фото перекадрированы через
  автоматическое распознавание лица (OpenCV) с одинаковой долей лица в
  кадре (~50% высоты кадра) и единым позиционированием по вертикали —
  теперь масштаб лица одинаков на всех карточках.
- **Высота карточек**: `.teacher-card` переведена на `display: flex;
  flex-direction: column`, `.teacher-info` — на `flex: 1`, а описание
  (`.teacher-info p`) зафиксировано на 2 строки (`-webkit-line-clamp: 2`)
  — раньше при разной длине текста карточки в одном ряду растягивались
  грид-сеткой до высоты самой длинной, и у карточек с более коротким
  текстом снизу оставалось пустое белое поле. Теперь высота card целиком
  и белая зона с текстом — идентичны на всех 6 карточках.
- Тексты описаний у Татьяны Николаевны и Анастасии Олеговны сокращены
  (были рассчитаны на 3 строки при реальной ширине карточки — до правки
  третья строка технически обрезалась бы line-clamp'ом) — сокращены так,
  чтобы помещаться в 2 строки без потери сути и без обрезки текста «Стаж —
  более N лет».
- Проверено скриншотами Playwright на 1440px / 820px / 390px — во всех
  трёх раскладках (3, 2 и 1 колонка) фото и текстовые блоки строго
  одинаковой высоты, обрезки текста нет.

**Раунд 10** — иллюстрация в hero обрезала детей по краям:
- `.hero-media-card img` был жёстко зафиксирован на `aspect-ratio: 4/3`,
  а сама иллюстрация (`images/hero-illustration.jpg`) — на самом деле
  `1000×584` (~12:7, заметно шире). При `object-fit: cover` в более узкую
  4:3-рамку браузер обрезал края слева и справа — а дети на этой картинке
  стоят почти вплотную к обеим границам кадра, поэтому крайних задевало.
- Первая попытка исправить (просто поменять `aspect-ratio` на `1000/584`)
  не сработала сама по себе: у `<img>` в HTML оставались атрибуты
  `width="700" height="525"` (старые пропорции 4:3), а CSS-правило
  переопределяло только `width`, но не `height` — значит побеждал
  прописанный в HTML `height="525"`, а не `aspect-ratio` в CSS.
  Добавлено явное `height: auto` в CSS-правило и HTML-атрибуты картинки
  обновлены на реальные `width="1000" height="584"` — теперь высота
  действительно считается из `aspect-ratio`, и `object-fit: cover`
  показывает иллюстрацию целиком, без обрезки по бокам.
- Проверено скриншотами Playwright (1440px и 390px) — все дети на
  иллюстрации, включая крайних слева и справа, видны полностью.

## 7. Локальная проверка перед деплоем

Сайт полностью статический, поэтому для просмотра достаточно любого простого
HTTP-сервера в папке проекта, например:

```bash
npx serve tropinka-landing
```

Функция `/api/submit` работает только на инфраструктуре Cloudflare Pages —
локально проверить полный цикл отправки в Telegram можно через
`npx wrangler pages dev tropinka-landing`.
